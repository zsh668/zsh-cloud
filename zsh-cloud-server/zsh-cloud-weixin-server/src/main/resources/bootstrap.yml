spring:
  application:
    name: zsh-cloud-weixin-server

#  cloud:
#    nacos:
#      config:
#        server-addr: 127.0.0.1:8848
#        file-extension: yaml
#        enabled: true

  boot:
    admin:
      client:
        url: http://127.0.0.1:5001/monitor
        username: admin
        password: admin
        instance:
          service-host-type: ip

management:
  health:
    redis:
      enabled: false
  endpoint:
    health:
      show-details: always
  endpoints:
    web:
      exposure:
        include: "*"
